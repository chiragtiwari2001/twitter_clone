<% if following?(user, other_user)%>
  <%= button_to "Unfollow", unfollow_user_path(other_user), class: "btn btn-light rounded-pill", method: :delete %>
<% else %>
  <%= button_to "Follow", follow_user_path(other_user), class: "btn btn-outline-light rounded-pill", method: :post %>
<% end %>